import React from 'react';
import { GlobalContextProps } from '@/context/interface';

// eslint-disable-next-line @typescript-eslint/no-empty-function
export const GlobalContext = React.createContext<GlobalContextProps>({});
